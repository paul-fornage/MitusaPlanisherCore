// Created by paulw on 5/8/2025.

#include "HmiReg.h"

// Explicit specialization for uint16_t (optional, not strictly necessary)
template class HmiReg<uint16_t>;

// Explicit specialization for bool (optional, not strictly necessary)
template class HmiReg<bool>;
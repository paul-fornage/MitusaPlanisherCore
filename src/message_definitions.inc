

// Intentionally meant to be able to be called twice
// ReSharper disable once CppMissingIncludeGuard
DEFINE_MESSAGE(WatchDogTimeout, "Watchdog alert. Main loop has not been executed in at least 1000ms")
DEFINE_MESSAGE(Blank, "")